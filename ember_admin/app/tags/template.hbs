<div class="container">
  <div class="col-md-3">
    {{#link-to 'tags.new' tagName="button"}}New{{/link-to}}
    <ul>
      {{#each model as |tag|}}
        <li>{{#link-to 'tag.show' tag}}{{tag.name}}{{/link-to}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="col-md-9">
    {{outlet}}
  </div>
</div>