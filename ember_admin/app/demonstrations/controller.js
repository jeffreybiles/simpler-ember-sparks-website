import Ember from 'ember';

export default Ember.Controller.extend({
  actions: {
    receiveAdvice(){
      alert('Make sure your customers are happy and your business model is sound.')
    }
  }
})
