<section>
  <div class="jumbotron">
    <div class="container">
      <h1>What do you want to learn today?</h1>
    </div>
  </div>
</section>

<section>
  <div class="container">
    <div class="card-series">
          <% @series.each do |series| %>
            <div class="card-well">
                <%= link_to series, class: 'link-but-normal' do %>

                  <!-- Series Image used here -->
                  <%= image_tag series.thumbnail_image, class: "img-responsive" %>

                  <div class="details">
                    <h4 class="media-heading"><%= series.name %></h4>
                    <%= series.taggings.length %> videos in the series
                  </div>
                <% end %>
            </div>
          <% end %>
    </div>
  </div>
</div>

<% content_for :title, "Explore- EmberScreencasts" %>
<% content_for :meta_description, "View Ember.js videos, sorted by subject." %>
      <!-- <div class="col-md-3 col-sm-4 col-xs-12">
        <br>
        <div id="tag_cloud">
          <% tag_cloud @subjects, %w[s m l] do |tag, css_class| %>
            <%= link_to tag.name, tag_path(tag), class: css_class %>
          <% end %>
        </div>
      </div> -->
